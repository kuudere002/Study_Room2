<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.study_room2.mapper.TodoMapper">
    <select id="findByUserId" resultType="Todo">
        SELECT * FROM todo WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>
    <select id="findById" resultType="Todo">
        SELECT * FROM todo WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todo (user_id, content, completed, created_at)
        VALUES (#{userId}, #{content}, 0, NOW())
    </insert>
    <update id="update">
        UPDATE todo SET content = #{content}, completed = #{completed} WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE FROM todo WHERE id = #{id}
    </delete>
</mapper>
